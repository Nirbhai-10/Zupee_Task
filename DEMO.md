# Saathi · Demo Runbook

> Empty until Day 6. Stub kept here so the file path is stable when the build week's automation references it.

## What lives here when complete

- 90-second demo script (T+0 → T+95s, beat by beat).
- Live mode vs replay mode switching.
- Reset between presentations (`scripts/reset-demo.sh`).
- Pre-rendered audio assets index.
- Backup plans: what to do if WiFi dies / Sarvam is rate-limiting / a phone refuses to play audio.

## Day 1

Foundation booted. `/` renders the design tokens. `/api/health` reports the configured stack. The presenter panel and replay surface are stubs — tracked for Day 6.
