import { DayPlaceholder } from "@/components/shared/DayPlaceholder";

export default function PresenterPage() {
  return (
    <DayPlaceholder
      day={6}
      title="Presenter control panel"
      whatToExpect="Trigger MIL scam, ULIP doc upload, plan generation, salary day cascade. Reset demo state. Switch live ↔ replay."
    />
  );
}
