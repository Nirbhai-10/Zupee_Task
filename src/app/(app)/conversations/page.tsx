import { DayPlaceholder } from "@/components/shared/DayPlaceholder";

export default function ConversationsPage() {
  return (
    <DayPlaceholder
      day={2}
      title="Conversations"
      whatToExpect="Mirror of the WhatsApp thread on web — full history with composer (mic, text, attachment)."
    />
  );
}
