import { DayPlaceholder } from "@/components/shared/DayPlaceholder";

export default function TimelinePage() {
  return (
    <DayPlaceholder
      day={5}
      title="Timeline"
      whatToExpect="Unified activity feed across defenses, investments, family, goals, and conversations."
    />
  );
}
