import { DayPlaceholder } from "@/components/shared/DayPlaceholder";

export default function NewGoalPage() {
  return (
    <DayPlaceholder
      day={4}
      title="Voice goal creator"
      whatToExpect="120px mic, 4–7 question intake, allocation animation, UPI Autopay mandate simulator."
    />
  );
}
